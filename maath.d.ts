declare module 'maath/random/dist/maath-random.esm' {
  export function someFunction(...args: any[]): any;
  export function inSphere(array: Float32Array, options?: { radius?: number }): Float32Array;
   // Ajusta según las funciones que necesites
  // Añade más funciones según lo que uses del módulo
}
